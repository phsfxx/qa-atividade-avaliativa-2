services:

  app:
    build: .

    container_name: laravel_app

    ports:
      - "8000:8000"

    working_dir: /app

    command: bash -c "composer install && php artisan serve --host=0.0.0.0 --port=8000"

    volumes:
      - .:/app